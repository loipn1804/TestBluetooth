<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnEnable"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:text="Enable" />

    <Button
        android:id="@+id/btnDiscoverable"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_toRightOf="@+id/btnEnable"
        android:text="Discoverable" />

    <Button
        android:id="@+id/btnStartDiscover"
        android:layout_width="180dp"
        android:layout_height="50dp"
        android:layout_below="@+id/btnEnable"
        android:text="Start Discover" />

    <Button
        android:id="@+id/btnStopDiscover"
        android:layout_width="180dp"
        android:layout_height="50dp"
        android:layout_alignBaseline="@+id/btnStartDiscover"
        android:layout_toRightOf="@+id/btnStartDiscover"
        android:text="Stop Discover" />
</RelativeLayout>
